"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";
import type { PromoBenefit, PromoCode } from "@/lib/parties";
import { useLocale } from "@/app/components/LocaleProvider";
import LocaleToggle from "@/app/components/LocaleToggle";

type Stats = { users: number; parties: number; joins: number; redemptions: number };
const benefitValues: Record<string, string | number> = { beta_access: "beta", profile_cover: "beta", avatar_frame: "neon", chat_effect: "sparkle", name_color: "#c9ff05", badge: "beta", xp_multiplier: 1.25, party_creation: "enabled" };

export default function PromoConsole({ initialPromos, initialStats, canWrite }: { initialPromos: PromoCode[]; initialStats: Stats; canWrite: boolean }) {
  const [promos, setPromos] = useState(initialPromos); const [notice, setNotice] = useState(""); const [busy, setBusy] = useState(""); const { locale } = useLocale();
  const c = locale === "ru" ? { queue: "Очередь", kicker: "награды, доступ и лимиты", title: <>Промо<br /><span>команда.</span></>, lead: "Коды могут быть одноразовыми или многоразовыми, иметь срок и выдавать набор вещей для профиля.", profiles: "профили", parties: "создано тус", joins: "входов по инвайту", redemptions: "активаций", accounts: "аккаунтов в TUSA.game", through: "через промодоступ", guest: "гостевых присоединений", used: "использовано промокодов", new: "новый доступ", createTitle: "Выпустить код", code: "Промокод", mode: "Режим", single: "Одноразовый", multi: "Многоразовый", limit: "Лимит активаций", expiry: "Срок действия", benefits: "Что выдаёт", create: "Создать промокод", creating: "Создаём…", all: "все коды", system: "в системе", note: "Код с активациями нельзя удалить — только остановить.", active: "активен", paused: "на паузе", once: "один раз", many: "много раз", until: "до", access: "только доступ", pause: "Пауза", enable: "Включить", remove: "Удалить", created: "создан.", activated: "Код активирован.", deactivated: "Код поставлен на паузу.", deleted: "Код удалён.", removeConfirm: "Удалить промокод" } : { queue: "Waitlist", kicker: "rewards, access and limits", title: <>Promo<br /><span>control.</span></>, lead: "Codes can be one-time or reusable, have an expiry date, and grant a set of profile items.", profiles: "profiles", parties: "hangouts created", joins: "invite joins", redemptions: "activations", accounts: "accounts in TUSA.game", through: "via promo access", guest: "guest joins", used: "promo codes used", new: "new access", createTitle: "Issue a code", code: "Promo code", mode: "Mode", single: "One-time", multi: "Reusable", limit: "Activation limit", expiry: "Expiry date", benefits: "What it gives", create: "Create promo code", creating: "Creating…", all: "all codes", system: "in the system", note: "A code with activations cannot be deleted — pause it instead.", active: "active", paused: "paused", once: "one time", many: "many uses", until: "until", access: "access only", pause: "Pause", enable: "Enable", remove: "Delete", created: "created.", activated: "Code activated.", deactivated: "Code paused.", deleted: "Code deleted.", removeConfirm: "Delete promo code" };
  const benefitLabels: Record<PromoBenefit["type"], string> = locale === "ru" ? { beta_access: "бета-доступ", profile_cover: "обложка", avatar_frame: "рамка", chat_effect: "эффект чата", name_color: "цвет имени", badge: "ачивка", xp_multiplier: "XP-модификатор", party_creation: "создание тусы" } : { beta_access: "beta access", profile_cover: "profile cover", avatar_frame: "avatar frame", chat_effect: "chat effect", name_color: "name color", badge: "badge", xp_multiplier: "XP modifier", party_creation: "create hangout" };
  async function request(method: "POST" | "PATCH" | "DELETE", body: Record<string, unknown>) { const response = await fetch("/api/admin/promos", { method, headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json(); if (!response.ok) throw new Error(data.error ?? "Save failed."); return data; }
  async function createPromo(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const selected = Array.from(form.getAll("benefits")).map((type) => ({ type: String(type), value: benefitValues[String(type)] })); setBusy("create"); setNotice(""); try { const data = await request("POST", { code: form.get("code"), mode: form.get("mode"), maxRedemptions: form.get("maxRedemptions") || null, expiresAt: form.get("expiresAt") || null, benefits: selected }); setPromos((items) => [data.promo, ...items]); event.currentTarget.reset(); setNotice(`${c.code} ${data.promo.code} ${c.created}`); } catch (error) { setNotice(error instanceof Error ? error.message : "Create failed."); } finally { setBusy(""); } }
  async function toggle(promo: PromoCode) { setBusy(promo.id); setNotice(""); try { const data = await request("PATCH", { id: promo.id, status: promo.status === "active" ? "paused" : "active" }); setPromos((items) => items.map((item) => item.id === promo.id ? data.promo : item)); setNotice(data.promo.status === "active" ? c.activated : c.deactivated); } catch (error) { setNotice(error instanceof Error ? error.message : "Update failed."); } finally { setBusy(""); } }
  async function remove(promo: PromoCode) { if (!window.confirm(`${c.removeConfirm} ${promo.code}?`)) return; setBusy(promo.id); setNotice(""); try { await request("DELETE", { id: promo.id }); setPromos((items) => items.filter((item) => item.id !== promo.id)); setNotice(c.deleted); } catch (error) { setNotice(error instanceof Error ? error.message : "Delete failed."); } finally { setBusy(""); } }
  return <main className="admin-page promo-admin-page"><header className="admin-header"><Link href="/admin" className="admin-brand">TUSA<span>.game</span></Link><div><LocaleToggle /><Link href="/admin" className="admin-text-button">{c.queue}</Link><span className="admin-kicker">promo control room</span></div></header><section className="admin-hero promo-admin-hero"><div><span className="admin-kicker">{c.kicker}</span><h1>{c.title}</h1><p>{c.lead}</p></div></section><section className="admin-metrics" aria-label="Product metrics"><article><span>{c.profiles}</span><strong>{initialStats.users}</strong><small>{c.accounts}</small></article><article><span>{c.parties}</span><strong>{initialStats.parties}</strong><small>{c.through}</small></article><article><span>{c.joins}</span><strong>{initialStats.joins}</strong><small>{c.guest}</small></article><article><span>{c.redemptions}</span><strong>{initialStats.redemptions}</strong><small>{c.used}</small></article></section><section className="promo-console-grid">{canWrite ? <form className="promo-create-card" onSubmit={createPromo}><span className="admin-kicker">{c.new}</span><h2>{c.createTitle}</h2><label>{c.code}<input name="code" required minLength={3} maxLength={32} placeholder="SUMMER26" autoCapitalize="characters" /></label><label>{c.mode}<select name="mode" defaultValue="single"><option value="single">{c.single}</option><option value="multi">{c.multi}</option></select></label><label>{c.limit}<input name="maxRedemptions" type="number" min="1" inputMode="numeric" placeholder={c.multi} /></label><label>{c.expiry}<input name="expiresAt" type="date" /></label><fieldset className="promo-benefits"><legend>{c.benefits}</legend>{Object.entries(benefitLabels).map(([type, label]) => <label key={type}><input name="benefits" value={type} type="checkbox" />{label}</label>)}</fieldset><button className="admin-button admin-button--lime" disabled={busy === "create"}>{busy === "create" ? c.creating : c.create}</button></form> : <aside className="promo-create-card"><span className="admin-kicker">read only</span><h2>{locale === "ru" ? "Только просмотр" : "View only"}</h2><p>{locale === "ru" ? "Изменение промокодов недоступно для этой роли." : "This role cannot change promo codes."}</p></aside>}<section className="promo-list-card"><div className="admin-table-head"><div><span className="admin-kicker">{c.all}</span><h2>{promos.length} {c.system}</h2></div><p>{c.note}</p></div>{notice && <p className="admin-notice">{notice}</p>}<div className="promo-list">{promos.map((promo) => <article key={promo.id} className="promo-row"><div><strong>{promo.code}</strong><span>{promo.status === "active" ? c.active : c.paused} · {promo.mode === "single" ? c.once : c.many}{promo.expiresAt ? ` · ${c.until} ${new Intl.DateTimeFormat(locale === "ru" ? "ru-KZ" : "en-US", { dateStyle: "short" }).format(new Date(promo.expiresAt))}` : ""}</span><em>{promo.benefits.map((benefit) => benefitLabels[benefit.type]).join(" · ") || c.access}</em></div><b>{promo.usesCount} / {promo.maxRedemptions ?? "∞"}</b>{canWrite && <><button className="admin-button" onClick={() => toggle(promo)} disabled={busy === promo.id}>{promo.status === "active" ? c.pause : c.enable}</button><button className="admin-delete" onClick={() => remove(promo)} disabled={busy === promo.id || promo.usesCount > 0}>{c.remove}</button></>}</article>)}</div></section></section></main>;
}
