"use client";
import { useEffect,useRef } from "react";
import { useControllerGame } from "@/app/components/useControllerGame";
import { useStageGame } from "@/app/components/useStageGame";
type State={engine:string;game:"truth"|"never"|"pairs";phase:"choose"|"round"|"finished";round:number;prompt:string;mode:string;responses:Record<string,boolean>;pairs:string[][];viewerId?:string}; const empty=(game:State["game"]):State=>({engine:"server-v1",game,phase:game==="truth"?"choose":"round",round:0,prompt:"",mode:game==="never"?"never":game==="pairs"?"pair":"truth",responses:{},pairs:[]});
export default function SocialTool({sessionId,onSave,role,game}:{partyId:string;sessionId?:string|null;onSave:(score:number)=>void;role?:"stage"|"controller";game:State["game"]}){const host=role==="stage";const stage=useStageGame<State>(host?sessionId??null:null,()=>empty(game));const controller=useControllerGame<State>(!host?sessionId??null:null,empty(game));const state=host?stage.state:controller.state;const send=host?stage.sendAction:controller.sendAction;const complete=useRef(false);useEffect(()=>{if(!host||state.phase!=="finished"||complete.current)return;complete.current=true;stage.complete();onSave(state.round);},[host,onSave,stage,state.phase,state.round]);const title=game==="truth"?"Truth or Dare":game==="never"?"Never Have I Ever":"Random Pairs";return <section className="party-game-board game-board-enter"><span className="game-step">{title} · {state.round+1}</span>{state.phase==="choose"&&host&&<div className="game-primary-actions"><button className="demo-action demo-action--lime" onClick={()=>send("choose",{mode:"truth"})} type="button">Truth</button><button className="demo-action demo-action--pink" onClick={()=>send("choose",{mode:"dare"})} type="button">Dare</button></div>}{state.phase==="round"&&<><h3 className="game-prompt-swap">{state.prompt}</h3>{game==="pairs"&&<div className="pairs-grid">{state.pairs.map(pair=><article className="pair-card-pop" key={pair.join("-")}>{pair.map(id=><strong key={id}>{id.slice(-8)}</strong>)}</article>)}</div>}{game!=="pairs"&&<div className="game-primary-actions"><button className="demo-action demo-action--white" disabled={Boolean(state.responses[state.viewerId??""])} onClick={()=>send("respond",{value:false})} type="button">Skip</button><button className="demo-action demo-action--lime" disabled={Boolean(state.responses[state.viewerId??""])} onClick={()=>send("respond",{value:true})} type="button">Me</button></div>}{host&&<div className="game-primary-actions"><button className="demo-action demo-action--lime" onClick={()=>send("next")} type="button">Next</button><button className="demo-action demo-action--white" onClick={()=>send("finish")} type="button">Finish</button></div>}</>}{sessionId&&<span className="multiplayer-badge">LIVE</span>}</section>;}
