"use client";
import { useEffect, useRef, useState } from "react";
import { useControllerGame } from "@/app/components/useControllerGame";
import { useStageGame } from "@/app/components/useStageGame";
type Color="red"|"yellow"|"green"|"blue";type Card={id:string;color:Color|null;kind:"number"|"skip"|"reverse"|"draw2"|"wild"|"wild4";value?:number};type State={phase:"playing"|"finished";players:string[];hands:Record<string,Card[]>;drawPile:Card[];discard:Card[];currentIndex:number;activeColor:Color;winner:string;uno:string[];viewerId?:string};const empty=():State=>({phase:"playing",players:[],hands:{},drawPile:[],discard:[],currentIndex:0,activeColor:"red",winner:"",uno:[]});const label=(card:Card)=>card.kind==="number"?String(card.value):card.kind==="draw2"?"+2":card.kind==="wild4"?"+4":card.kind==="wild"?"W":card.kind==="skip"?"⊘":"↻";
export default function UnoTracker({sessionId,onSave,role}:{partyId:string;sessionId?:string|null;onSave:(score:number)=>void;role?:"stage"|"controller"}){const stageRole=role==="stage";const stage=useStageGame<State>(stageRole?sessionId??null:null,empty);const controller=useControllerGame<State>(!stageRole?sessionId??null:null,empty());const state=stageRole?stage.state:controller.state;const send=stageRole?stage.sendAction:controller.sendAction;const[color,setColor]=useState<Color>("red");const completed=useRef(false);const me=state.viewerId??"";const hand=state.hands[me]??[];const top=state.discard.at(-1);const myTurn=state.players[state.currentIndex]===me;useEffect(()=>{if(!stageRole||state.phase!=="finished"||completed.current)return;completed.current=true;stage.complete();onSave(1)},[onSave,stage,stageRole,state.phase]);return <section className="party-game-board game-board-enter uno-multiplayer"><span className="game-step">COLOR CARDS · {state.currentIndex+1}/{state.players.length}</span>{top&&<div className="uno-table"><button className="uno-card uno-card--back" disabled={!myTurn} onClick={()=>send("draw")} type="button">+</button><div className={`uno-card uno-card--${top.color??"wild"}`}><strong>{label(top)}</strong><small>{state.activeColor}</small></div></div>}<p>{myTurn?"Твой ход":"Ход другого игрока"}</p><div className="uno-color-choice">{(["red","yellow","green","blue"] as Color[]).map(item=><button aria-label={item} className={color===item?"active":""} key={item} onClick={()=>setColor(item)} style={{background:item}} type="button"/>)}</div><div className="uno-hand">{hand.map(card=><button className={`uno-card uno-card--${card.color??"wild"}`} disabled={!myTurn} key={card.id} onClick={()=>send("play",{cardId:card.id,color})} type="button"><strong>{label(card)}</strong></button>)}</div>{state.phase==="finished"&&<h3>{state.winner===me?"Ты выиграл!":"Победитель: "+state.winner.slice(-6)}</h3>}{sessionId&&<span className="multiplayer-badge">LIVE</span>}</section>}
